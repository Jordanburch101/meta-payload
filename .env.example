# Added by Payload
# Required for database connection - get this from your Supabase database settings
DATABASE_URI=postgresql://your-username:your-password@your-host:your-port/your-database
# Required - generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-actual-secret-key

# Supabase S3 Storage Configuration
# All of these can be found in your Supabase project settings under Storage
S3_BUCKET=your-actual-bucket-name
S3_ACCESS_KEY_ID=your-actual-access-key-id
S3_SECRET_ACCESS_KEY=your-actual-secret-access-key
S3_REGION=your-actual-region # e.g. ap-southeast-2
S3_ENDPOINT=your-actual-s3-endpoint # e.g. https://[project-ref].supabase.co/storage/v1/s3
S3_PUBLIC_ENDPOINT=your-actual-public-endpoint # e.g. https://[project-ref].supabase.co/storage/v1/object/public/[bucket-name]

# Vercel Configuration
# Use localhost for development, update for production
# Example prod: https://your-app.vercel.app
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Resend - Email Service
# Sign up at https://resend.com
# Required for sending emails (auth, notifications, etc)
RESEND_API_KEY=your-actual-resend-api-key

# Posthog Analytics
# Sign up at https://posthog.com
# Optional - for tracking user behavior
NEXT_PUBLIC_POSTHOG_KEY=your-actual-posthog-key
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Sentry Error Tracking
# Sign up at https://sentry.io
# Optional - for error monitoring and debugging
SENTRY_AUTH_TOKEN=your-actual-sentry-auth-token

# Additional Notes:
# 1. Never commit the actual .env file to version control
# 2. Make sure to add .env to your .gitignore file
# 3. For development, copy this file to .env and fill in your values
# 4. Make sure you add all of these variables to the deployment environment